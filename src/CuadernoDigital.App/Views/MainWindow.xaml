<Window x:Class="CuadernoDigital.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:CuadernoDigital.App.Controls"
        xmlns:services="clr-namespace:CuadernoDigital.App.Services"
        Title="ProNotes"
        Width="1080"
        Height="720"
        MinWidth="760"
        MinHeight="520"
        Background="#F7F8FA">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0"
                   Background="White"
                   LastChildFill="False"
                   Margin="0"
                   VerticalAlignment="Top">
            <StackPanel DockPanel.Dock="Left" Margin="22,16">
                <TextBlock Text="ProNotes"
                           FontSize="28"
                           FontWeight="SemiBold"
                           Foreground="#111827" />
                <TextBlock Text="Tus cuadernos digitales"
                           Foreground="#64748B"
                           Margin="1,2,0,0" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Right"
                        Orientation="Horizontal"
                        Margin="16"
                        VerticalAlignment="Center">
                <Button Content="Actualizar" Command="{Binding RefreshCommand}" />
                <Button Content="Nuevo cuaderno" Command="{Binding NewNotebookCommand}" Background="{StaticResource AccentBrush}" Foreground="White" />
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1" Margin="20">
            <ListBox ItemsSource="{Binding Notebooks}"
                     SelectedItem="{Binding SelectedNotebook}"
                     MouseDoubleClick="NotebookList_MouseDoubleClick"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="8" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Card"
                                            Background="White"
                                            BorderBrush="#CBD5E1"
                                            BorderThickness="1"
                                            CornerRadius="8">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Card" Property="BorderBrush" Value="#2563EB" />
                                            <Setter TargetName="Card" Property="BorderThickness" Value="2" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Card" Property="BorderBrush" Value="#94A3B8" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type services:NotebookSummary}">
                        <Grid Width="210" Height="260">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="150" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid>
                                <controls:CoverPreviewControl CoverTitle="{Binding Title}"
                                                              Subtitle="{Binding Subtitle}"
                                                              BackgroundColor="{Binding CoverColor}"
                                                              AccentColor="{Binding AccentColor}"
                                                              ImageBase64="{Binding CoverImageBase64}"
                                                              ImageOffsetX="{Binding CoverImageOffsetX}"
                                                              ImageOffsetY="{Binding CoverImageOffsetY}"
                                                              ImageScale="{Binding CoverImageScale}"
                                                              TemplateName="{Binding TemplateName}"
                                                              TitleColor="{Binding TitleColor}"
                                                              SubtitleColor="{Binding SubtitleColor}"
                                                              CornerRadius="8,8,0,0"
                                                              TitleFontSize="21"
                                                              SubtitleFontSize="13" />
                                <Button Content="..."
                                        Width="32"
                                        Height="28"
                                        Padding="0"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Top"
                                        Margin="0,8,8,0"
                                        Background="#D9FFFFFF"
                                        Tag="{Binding}"
                                        Click="NotebookMenu_Click">
                                    <Button.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Abrir"
                                                      Click="OpenNotebookMenuItem_Click" />
                                            <MenuItem Header="Editar portada"
                                                      Click="EditCoverMenuItem_Click" />
                                            <Separator />
                                            <MenuItem Header="Duplicar"
                                                      IsEnabled="False" />
                                            <MenuItem Header="Exportar PDF"
                                                      IsEnabled="False" />
                                            <Separator />
                                            <MenuItem Header="Eliminar"
                                                      Click="DeleteNotebookMenuItem_Click" />
                                        </ContextMenu>
                                    </Button.ContextMenu>
                                </Button>
                            </Grid>
                            <StackPanel Grid.Row="1" Margin="14">
                                <TextBlock Text="{Binding Title}"
                                           FontWeight="SemiBold"
                                           Foreground="#111827"
                                           TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{Binding UpdatedAt, StringFormat='Actualizado {0:g}'}"
                                           Foreground="#64748B"
                                           Margin="0,6,0,0" />
                            </StackPanel>
                            <Button Grid.Row="2"
                                    Content="Abrir"
                                    HorizontalAlignment="Right"
                                    Margin="8"
                                    Command="{Binding DataContext.OpenNotebookCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Background="White"
                    BorderBrush="#D8DEE9"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="28"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Notebooks.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <TextBlock Text="No hay cuadernos todavia"
                               FontSize="20"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="Crea el primero para empezar a escribir o dibujar."
                               Foreground="#64748B"
                               Margin="0,8,0,14"
                               HorizontalAlignment="Center" />
                    <Button Content="Crear cuaderno"
                            Command="{Binding NewNotebookCommand}"
                            Background="{StaticResource AccentBrush}"
                            Foreground="White"
                            HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
