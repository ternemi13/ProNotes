<Window x:Class="CuadernoDigital.App.Views.TableSizeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Insertar tabla"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">
    <Grid Margin="18" Width="320">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Tamaño de la tabla"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="4,0,4,12" />

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Margin="4">
                <TextBlock Text="Filas" Margin="0,0,0,4" />
                <ComboBox x:Name="RowsBox" MinHeight="32" />
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="4">
                <TextBlock Text="Columnas" Margin="0,0,0,4" />
                <ComboBox x:Name="ColumnsBox" MinHeight="32" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="2"
                BorderBrush="#CBD5E1"
                BorderThickness="1"
                Padding="10"
                Margin="4"
                CornerRadius="6">
            <Grid x:Name="PreviewGrid"
                  Width="220"
                  Height="120" />
        </Border>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Cancelar"
                    IsCancel="True"
                    MinWidth="86" />
            <Button Content="Insertar"
                    IsDefault="True"
                    MinWidth="86"
                    Click="Accept_Click" />
        </StackPanel>
    </Grid>
</Window>
